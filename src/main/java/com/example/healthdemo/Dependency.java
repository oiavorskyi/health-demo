package com.example.healthdemo;

/**
 * Primitive emulation of external dependency state. In real application
 * the state would be rather deducted based on the interactions with the
 * dependency.
 */
public class Dependency {
    public static boolean isUp = true;
}
